# Backend Environment Variables (สำหรับรันโดยตรง ไม่ผ่าน Docker)

# ========================================
# Application Configuration
# ========================================
NODE_ENV=development
PORT=4000

# ========================================
# Database Configuration
# ========================================
# For local development (not Docker)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=gallery_user
DB_PASSWORD=gallery_pass_2024
DB_DATABASE=gallery_db

MAX_KEYWORDS_COUNT=10

API_DELAY=2000  # 2 วินาที